# GRUB configuration for Base Kernel

# Set default boot entry
set default=0
set timeout=1

# Disable graphics mode
set gfxpayload=text

# Boot menu
menuentry "Base Kernel" {
    # Load the kernel with Multiboot
    insmod multiboot2
    multiboot2 /boot/kernel.elf

    # Boot the kernel
    boot
}
